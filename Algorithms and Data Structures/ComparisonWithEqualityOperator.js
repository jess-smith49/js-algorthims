//these operators return a boolean true or false value
//in order for js to compare two different data types (nubers and strings) it must convert one type to another
//This is type Coercion
function equalityTest(myVal) {
    if(myVal == 10){
        return "Equal"
    }

    return "Not Equal"
}

//equalityTest(10);
//equalityTest(12);
