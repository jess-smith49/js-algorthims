//the inequality operator is the opposit of th equality operator
//not equal and returns false 
//will convert data types of values

function testNotEqual(val) {
    if (val != 99) { 
      return "Not Equal";
    }
    return "Equal";
  }
  
  testNotEqual(10);