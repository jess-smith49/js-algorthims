/*
.shift() is similar to .pop() except it removes the first element of the array instead
of the last element
*/

var employee = ["jessica", "smith", ["manager"]];
var removedFromArray = employee.shift();